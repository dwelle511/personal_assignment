# 🤗 Hugging Face Quickstart: AI 모델 사용 가이드

[![Hugging Face](https://img.shields.io/badge/%F0%9F%A4%97-Hugging%20Face-yellow?style=flat-square)](https://huggingface.co/)
[![Python](https://img.shields.io/badge/Python-3.8%2B-blue?style=flat-square&logo=python)](https://www.python.org/)

**Hugging Face Hub**는 전 세계의 AI 모델과 데이터셋이 모이는 오픈소스 플랫폼입니다.
이 문서는 **설치부터 모델 사용, 공유까지**의 핵심 과정을 한 페이지로 요약했습니다.

---

## 🛠️ 1. 환경 설정 (Installation)
Python 환경에서 딥러닝 프레임워크와 Hugging Face 필수 라이브러리를 설치합니다.

```bash
# 1. PyTorch 설치 (시스템에 맞게 변경 권장: [https://pytorch.org/](https://pytorch.org/))
pip install torch torchvision torchaudio

# 2. Hugging Face 라이브러리 설치
pip install transformers datasets huggingface_hub
```

## 🔑 2. 로그인 (Authentication)
모델 업로드 및 Private 저장소 접근을 위해 터미널에서 로그인합니다. (토큰 발급: Settings > Access Tokens)

```
# 터미널에서 아래 명령어 실행
huggingface-cli login

# "Token:" 프롬프트가 뜨면 발급받은 토큰(Write 권한)을 붙여넣으세요.
# (보안을 위해 화면에는 입력된 문자가 보이지 않습니다.)
```

## ⚡ 3. 모델 바로 사용하기 (Inference)
pipeline을 사용하면 코드 3줄로 최신 AI 모델을 다운로드하고 실행할 수 있습니다.
```
from transformers import pipeline

# 모델 자동 다운로드 및 실행
classifier = pipeline("sentiment-analysis")

# 결과 확인
result = classifier("I love using Hugging Face! It's amazing.")
print(result)
# 출력: [{'label': 'POSITIVE', 'score': 0.9998}]
```

### 📝 텍스트 감성 분석
```
from transformers import pipeline

# 모델 자동 다운로드 및 실행
classifier = pipeline("sentiment-analysis")

# 결과 확인
result = classifier("I love using Hugging Face! It's amazing.")
print(result)
# 출력: [{'label': 'POSITIVE', 'score': 0.9998}]
```
### 🖼️ 이미지 분류 (Vision)
(실행 전 pip install pillow 필요)
```
from transformers import pipeline
from PIL import Image
import requests

# 온라인 이미지 불러오기
url = "[http://images.cocodataset.org/val2017/000000039769.jpg](http://images.cocodataset.org/val2017/000000039769.jpg)"
image = Image.open(requests.get(url, stream=True).raw)

# 이미지 분류 모델 실행
classifier = pipeline("image-classification", model="google/vit-base-patch16-224")
print(classifier(image))
# 출력: [{'score': 0.99, 'label': 'Egyptian cat'}, ...]
```

## 📊 4. 데이터셋 불러오기 (Datasets)
수만 개의 오픈 데이터셋을 로컬 용량 걱정 없이 스트리밍으로 로드합니다.
```
from datasets import load_dataset

# IMDB 영화 리뷰 데이터셋 로드
dataset = load_dataset("imdb")

# 첫 번째 데이터 확인
print(dataset['train'][0])

```

## 🚀 5. 내 모델 공유하기 (Upload)

학습된 모델을 내 저장소(Hub)에 업로드하여 공유할 수 있습니다.

```
from transformers import AutoModelForSequenceClassification, AutoTokenizer

# 1. 모델과 토크나이저 준비 (예시)
model_name = "distilbert-base-uncased-finetuned-sst-2-english"
model = AutoModelForSequenceClassification.from_pretrained(model_name)
tokenizer = AutoTokenizer.from_pretrained(model_name)

# 2. Hub로 업로드 (사전에 로그인 필요)
repo_id = "my-username/my-first-bert"  # 본인의 유저네임으로 변경

model.push_to_hub(repo_id)
tokenizer.push_to_hub(repo_id)

print(f"업로드 완료: [https://huggingface.co/](https://huggingface.co/){repo_id}")
```
