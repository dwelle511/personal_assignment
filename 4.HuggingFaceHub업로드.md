# ☁️ Hugging Face Hub Upload Guide

[![Hugging Face](https://img.shields.io/badge/%F0%9F%A4%97-Hugging%20Face-yellow?style=flat-square)](https://huggingface.co/)
[![Python](https://img.shields.io/badge/Python-3.8%2B-blue?style=flat-square&logo=python)](https://www.python.org/)

이 문서는 로컬에서 작업한 모델이나 파일을 Hugging Face Hub 저장소(Repository)로 업로드하는 3가지 핵심 방법을 설명합니다.

---

## ✅ 1. 사전 준비 (Prerequisites)

업로드를 하려면 **쓰기(Write) 권한이 있는 토큰**과 로그인이 필수입니다.

1.  **토큰 발급:** [Settings > Access Tokens](https://huggingface.co/settings/tokens)에서 'Write' 권한으로 토큰 생성.
2.  **로그인:**

```bash
pip install huggingface_hub

# 터미널에서 실행 후 토큰 입력
huggingface-cli login
```
## 🚀 2. Transformers 모델 업로드 (가장 쉬운 방법)

transformers 라이브러리로 학습한 모델은 push_to_hub() 메서드 하나로 업로드가 가능합니다.
```
from transformers import AutoModelForSequenceClassification, AutoTokenizer

# 1. 모델과 토크나이저 로드 (또는 학습 완료된 객체)
model_name = "bert-base-uncased"
model = AutoModelForSequenceClassification.from_pretrained(model_name)
tokenizer = AutoTokenizer.from_pretrained(model_name)

# 2. Hub로 푸시 (자동으로 저장소 생성 및 업로드)
# repo_id: "사용자명/저장소명" 형식
repo_id = "my-username/my-awesome-bert"

model.push_to_hub(repo_id)
tokenizer.push_to_hub(repo_id)

print(f"Upload Success: [https://huggingface.co/](https://huggingface.co/){repo_id}")
```

## 📂 3. 일반 파일/폴더 업로드 (Hugging Face SDK)

모델 파일뿐만 아니라, 일반적인 파일이나 폴더 전체를 업로드할 때 huggingface_hub 라이브러리를 사용합니다.
```
from huggingface_hub import HfApi

api = HfApi()
repo_id = "my-username/my-custom-dataset"
user_token = "YOUR_WRITE_TOKEN" # 로그인 되어 있다면 생략 가능

# 1. 저장소 생성 (없을 경우)
api.create_repo(repo_id=repo_id, exist_ok=True, repo_type="model")

# 2. 폴더 전체 업로드
# folder_path: 로컬 폴더 경로
# path_in_repo: Hub 저장소 내의 경로 (루트는 '.')
api.upload_folder(
    folder_path="./local_checkpoint",
    repo_id=repo_id,
    repo_type="model",  # 'dataset' 또는 'space'로 변경 가능
    commit_message="Upload formatting checkpoint"
)

print("Folder uploaded successfully!")
```
## 🐙 4. Git LFS 사용 (대용량 파일 관리)

터미널에서 Git 명령어를 사용하여 관리하고 싶을 때 사용하는 전통적인 방법입니다. 10MB 이상의 파일은 git-lfs가 필수입니다.
```
# 1. Git LFS 설치 (최초 1회)
git lfs install

# 2. 저장소 클론 (Hub에서 먼저 저장소를 생성해야 함)
git clone [https://huggingface.co/my-username/my-repo](https://huggingface.co/my-username/my-repo)
cd my-repo

# 3. 파일 복사 및 트래킹 설정
cp ../my_model.bin .
git lfs track "*.bin"  # 대용량 파일 확장자 추적

# 4. 커밋 및 푸시
git add .
git commit -m "Add model weights"
git push
```

## 📝 5. Model Card 작성 (중요)

업로드가 완료되면 Hub 웹페이지의 해당 저장소로 이동하여 README.md를 수정해야 합니다. 이를 Model Card라고 합니다.

- Model Details: 어떤 모델인지?

- Intended Use: 어디에 쓰는 것인지?

- Training Data: 무엇으로 학습했는지?

- Tags: (YAML 메타데이터) 검색이 잘 되도록 태그 추가.

```
---
language: ko
tags:
- text-classification
- pytorch
license: mit
---

# My Awesome BERT

이 모델은 한국어 감성 분석을 위해 학습되었습니다...
```
